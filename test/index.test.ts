import {describe, test} from "@jest/globals";
import {RandomUtils} from "../src";

describe("StringUtils Test", () => {
  test("isEmpty Test", () => {
    console.log(RandomUtils.nextFloatArray(5, 5, 10));
    console.log(RandomUtils.nextNoRepeatFloatArray(5, 5, 10));
  });
});
